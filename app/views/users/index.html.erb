

<h1> Users </h1>

<ul>
	<% @users.each do |user| %>
		<li>
			<!-- 
				name: <a href="/users/ <%= user.id %> "><%= user.name %></a></br>
				<%= render partial: 'user', locals: { user: user} %> 
			-->
			<%= render user %>
		</li>
	<% end %>	
</ul>